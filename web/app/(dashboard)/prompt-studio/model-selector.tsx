// Model selector removed — single model configured on backend via OPENROUTER_DEFAULT_MODEL env var.
// This file kept as stub to avoid import errors from other pages.

export function ModelSelector() {
  return null;
}